services:
  client:
    build:
      context: client
      dockerfile: Dockerfile
    ports:
      - 3000:3000

  server:
    build:
      context: server
      dockerfile: Dockerfile
    dns:
      - 1.1.1.1
      - 8.8.8.8
    env_file:
      - server/.env
    ports:
      - 8000:8000
